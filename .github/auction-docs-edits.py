# Temporary source transport. Every baseline and result is checked before writing.
import pathlib,hashlib
writes=[]
def patch(name,before,after,edits):
    p=pathlib.Path(name)
    text=p.read_text() if p.exists() else ''
    assert hashlib.sha256(text.encode()).hexdigest()==before, 'Unexpected baseline: '+name
    lines=text.splitlines(keepends=True)
    for start,end,value in reversed(edits):
        assert 0<=start<=end<=len(lines),name
        lines[start:end]=[value]
    text=''.join(lines)
    assert hashlib.sha256(text.encode()).hexdigest()==after, 'Result differs from tested source: '+name
    writes.append((p,text))

patch('README.md','0e31ebd63c333f8ab09c4ce88884eb65e022a5079622fb96dd12abe3502c0e82','1e1c36a6f2d1513cd7001aca1b542d79d97e9dab6c3c49610ea35cbd2d19a592',[
(4,5,r'''
这是根据项目对话记录重新实现的单机 Web RPG，版本 **1.6.0**，重建日期 **2026-09-18**。原账号读取返回 `403: Sorry. Your account was suspended`，没有取得旧仓库源码。因此，这是**玩法与需求重建**，不是原始代码、界面和剧情文本的逐字恢复。
'''[1:]),
(10,11,r'''
## 1.6.0：万宝拍卖场（最新）

万宝拍卖场设在长乐集二楼，沿用原地图节点与真实道路移动，不增加瞬移。已开放炼药且累计完成 10 单配送后，导航栏出现「拍卖」，长乐集也出现服务入口；未达条件时隐藏，直接调用竞拍指令同样会被拒绝。

每日 18:00–24:00 开拍，白天到场可提前查看当日三件拍品。药材成包、成品丹药与当前境界可用的药鼎进入图录；玩家尚未买过第一口鼎或当前境界药鼎已收齐时，第三件改为另一包药材。拍卖不出售丹方，不改变剧情、羁绊、商店各 36 张的专属规则。药鼎竞得后加入收藏，不自动换鼎；第一口药鼎仍须在长乐集炼药摊购买。

当日第一次查看图录时，固定拍品和三位匿名竞拍者的预算；重开面板和读档不换货、不重抽对手。对手是拍卖背景角色，不自动加入羁绊。药材参考价沿用商店售价，药鼎参考价沿用购鼎价格；成品丹药参考价根据品类、阶数与数量计算。起拍价为参考价的 65% 向上取整到 5 元，每口为参考价的 8% 向上取整到 5 元且至少 5 元；这不保证最终买到的价格低于商店。

每次可「加一口」或「加三口」，一轮竞价持续 5 个游戏分钟。出价先将全额现金转为冻结款，暂停、倍率与其他行动共用同一时钟；提交后本轮不可中断，不允许同时出价两件拍品。对手超价时全额返还冻结款，再选择继续加价或免费放弃；无人继续竞争时才落槌，冻结款转货款并发放拍品，不再重复扣款。每轮只在真正完成时结算，没有按帧随机报价。距散场不足一轮时禁止新出价；最后一轮被超价则退款并结束该拍品。

冻结款不是可用现金，不能用于支付房租。第 7、14、21… 日午夜依然先扣租，余额不足会先结束游戏，不能预支同刻拍卖退款或成交物品。资金上限内预留退款空间，正在竞拍的物品也预留库存容量。历史竞价与成交记录只写右下角日志；面板只显示当前图录、状态、报价和累计竞得数量。

存档升为 v11，保存图录、对手预算、当前轮次、冻结款与成交状态；v10 及更早存档显式迁移为空拍卖记录，保留原有玩法、物品和在途行动。进行中或待选竞拍重载后可继续，已成交拍品不重发；缺失冻结款关联、非法物品或轮次会拒绝读取并保留损坏存档，不静默清空吞款。新旧存档各自保存拍卖数据，读档默认暂停，无关闭页面后的离线拍卖。

10 单、每日三件、开拍时间、五分钟轮次及报价比例是本轮采用的配置数值，不是用户指定的固定数值。经典和 AI 模式共用确定性拍卖规则，AI 不生成竞价金额或结算奖励。

## 1.5.0：玩法与导航渐进开放（历史）
'''[1:]),
])

patch('PROJECT_MEMORY.md','473d0965d8e13c7e6e1082ba76573bd3ba7f47f4ba34091b76a95f2351dfdbe7','cd1639f4dc4b26bc85a8e299ee2a69243c7ce81cc17979574514e141c8c98fb9',[
(107,108,r'''
## 2026-09-19（1.6.0）：万宝拍卖场（最新）

万宝拍卖场设在长乐集二楼，沿用原地图节点与真实道路移动，不增加瞬移。已开放炼药且累计完成 10 单配送后，导航栏出现「拍卖」，长乐集也出现服务入口；未达条件时隐藏，直接调用竞拍指令同样会被拒绝。

每日 18:00–24:00 开拍，白天到场可提前查看当日三件拍品。药材成包、成品丹药与当前境界可用的药鼎进入图录；玩家尚未买过第一口鼎或当前境界药鼎已收齐时，第三件改为另一包药材。拍卖不出售丹方，不改变剧情、羁绊、商店各 36 张的专属规则。药鼎竞得后加入收藏，不自动换鼎；第一口药鼎仍须在长乐集炼药摊购买。

当日第一次查看图录时，固定拍品和三位匿名竞拍者的预算；重开面板和读档不换货、不重抽对手。对手是拍卖背景角色，不自动加入羁绊。药材参考价沿用商店售价，药鼎参考价沿用购鼎价格；成品丹药参考价根据品类、阶数与数量计算。起拍价为参考价的 65% 向上取整到 5 元，每口为参考价的 8% 向上取整到 5 元且至少 5 元；这不保证最终买到的价格低于商店。

每次可「加一口」或「加三口」，一轮竞价持续 5 个游戏分钟。出价先将全额现金转为冻结款，暂停、倍率与其他行动共用同一时钟；提交后本轮不可中断，不允许同时出价两件拍品。对手超价时全额返还冻结款，再选择继续加价或免费放弃；无人继续竞争时才落槌，冻结款转货款并发放拍品，不再重复扣款。每轮只在真正完成时结算，没有按帧随机报价。距散场不足一轮时禁止新出价；最后一轮被超价则退款并结束该拍品。

冻结款不是可用现金，不能用于支付房租。第 7、14、21… 日午夜依然先扣租，余额不足会先结束游戏，不能预支同刻拍卖退款或成交物品。资金上限内预留退款空间，正在竞拍的物品也预留库存容量。历史竞价与成交记录只写右下角日志；面板只显示当前图录、状态、报价和累计竞得数量。

存档升为 v11，保存图录、对手预算、当前轮次、冻结款与成交状态；v10 及更早存档显式迁移为空拍卖记录，保留原有玩法、物品和在途行动。进行中或待选竞拍重载后可继续，已成交拍品不重发；缺失冻结款关联、非法物品或轮次会拒绝读取并保留损坏存档，不静默清空吞款。新旧存档各自保存拍卖数据，读档默认暂停，无关闭页面后的离线拍卖。

10 单、每日三件、开拍时间、五分钟轮次及报价比例是本轮采用的配置数值，不是用户指定的固定数值。经典和 AI 模式共用确定性拍卖规则，AI 不生成竞价金额或结算奖励。

## 2026-09-19（1.5.0）：玩法与导航渐进开放（历史）
'''[1:]),
(159,160,r'''
版本 1.6.0，存档格式 11。支持识别的重建版 v1–v10 字段迁移；v10/v9/v8/v7/v6/v5/v4/v3 本机存储在新键不存在时迁到 v11，保留 110 地点、10 分钟充电、住处、车辆、人物关系、事件和途中行动。旧档没有小境界字段时从原大境界一重继续；旧炼药途中状态可补基础药鼎与当前丹方，v8 旧档按旧规则已经可用的丹方迁移为已获得。未知原版存档没有迁移适配。当前仓库为 `meviu523/daoist`；没有云存档、多人、权威后端、防作弊或真实 AI 提供方联调。
'''[1:]),
])

patch('docs/ARCHITECTURE.md','01fe9b3a704caccf048cce40da02240bed06d98b896b77fe21a16beef0a265d1','20fdd50f51f06139ef19575714fa3f4755972171db03711d5d2788bd1f6a696e',[
(30,31,r'''
同时只能有一个 `activity`。`stop` 保留当前位置、实际消耗和已经获得的渐进收益；已接单继续计时，使用 `resumeDelivery` 继续，或 `cancelDelivery` 明确取消且无报酬。停止不是暂停。执行行动期间只允许可中断行为停止以及零耗时 `buy/sign/claim`；已提交的拍卖轮次与耗时事件选择不可停止；换交通方式、服药和另一个耗时行动须先停下。
'''[1:]),
(34,35,r'''
`data.js` 的 `FEATURE_UNLOCKS` 是功能 ID、门槛、前置、提示与解锁日志的唯一配置；`FEATURE_PANELS` 映射系统、修行、炼药、羁绊与拍卖五个受限面板。新档仅显示行囊、座驾、歇息、帮助；地图接单、基础生存、时间控制、日志、角色资料和存档不受门槛限制。初始物品可继续通过行囊使用。
'''[1:]),
(36,37,r'''
功能顺序为：首单实际交付结算 → 系统；领取 q1 → 修行/吐纳；完整修炼一次 → 进阶功课/突破/探索；已开放修行且送达至少三单、street-vein 主线完成 → 炼药；五单 → 升级；已开放炼药且十单 → 拍卖；二十单 → 系统归途。羁绊独立在遇见第一位人物时开放。前置配置只用于必要依赖，不强迫玩家完成所有其他玩法；比如可以先送三单再炼药，不要求先走情感线。
'''[1:]),
(45,45,r'''

## 万宝拍卖场（1.6.0）

`AUCTION` 保存地点 market、开放 18:00–24:00、每日三件拍品、每轮五分钟、起拍/加价比例和预算步数上限；`AUCTION_BIDDERS` 是三位匿名竞拍者，不复用 NPC 羁绊。功能配置要求 `alchemy` 前置与十单实际交付，导航、市场快捷卡和核心指令共用门槛。没有新增或移动地图节点，异地入口只能查看已缓存图录并规划真实路程，到场才能生成当日图录或出价。

`auctionCatalog` 是零耗时事务，首次到场查看当日图录时才调用世界 RNG：药材包（3–5）、成品丹药（1–2），以及未拥有、境界允许的药鼎（1）；未购过药鼎或当前境界无缺失鼎时，第三件换另一包药材。品阶境界使用与炼药素材共用的 `ALCHEMY_REALM_BY_TIER`。不产生丹方、独占功法或人物解锁。当天图录不随面板、帧、读档或境界提升再抽，新一天首次查看替换旧图录，但保留累计成交数量与支出。

`auctionLotInfo` 为价格与展示唯一来源：药材/鼎沿用原现金价格；丹药单价为 60+25×阶数，破境类再加60，乘数量得参考价。起拍为参考价×0.65向上取整至5元；加价步长为参考价×0.08向上取整至5元且至少5元。每位对手的隐藏预算为起拍价加 0–8 个步长，创建图录时一次确定。对手行为与画面帧率、AI、刷新次数无关，不承诺竞得价总比固定商店便宜。

`auctionBid` 或拍卖待选事件中的继续竞价先原子校验地点、开放时间、剩余完整轮次、资格、所有权、库存容量、报价和现金，再全额冻结资金并建立 `activity.kind='auction'`。一口/三口分别加一个/三个步长（首拍以起拍价计第一口）。一轮5分钟只由 `G.advance` 推进，提交后不可中断；`startAuctionBid` 不发奖励、不跳时间。其他拍品与当地行动不能并发；保留系统 buy/sign/claim 即时事务，但限制同物品兑换以预留即将成交的库存空间，现金上限也减去冻结款以保留完整退款空间。

`finishAuctionBid` 在实际完成节点检查预先封存的预算：有对手能再加一步时，轮流选符合预算的对手抬价、退还全额冻结款，并生成经典三选项 `pending.kind='auction'`（一口/三口/免费退出）；无对手则落槌，将冻结款结转为支出，增加对应物品或药鼎收藏并累计 wins/spent，不再次扣现金、不自动换鼎。最后一轮超价且无时间再开轮时退款并标记 lost，不产生过期事件。待选时沿用全局事件暂停，选择后才开始新一轮；成交/退出自动返回拍卖面板，不增加独立计时器。

世界跨日先收租，冻结款不算可用现金；午夜破产会中止同刻竞价结束，不发退款或拍品。只在成功完成节点生成对应日志，拍卖面板不重复列历史日志。时钟文本容忍 1e-8 分钟的积分浮点尾差，避免18:05的完成节点显示18:04，但不改变状态时间或任何结算门槛。

`auction={day,realm,lots,activeId,wins,spent}`；每件 lot 保存 `{id,kind,item,count,status,price,leader,held,round,rivals}`，status 为 ready/bidding/decision/won/lost，对手只存白名单ID和ceiling。v11 的 `cleanAuction` 校验每日数量、境界、物品、步长/预算、状态/冻结款、所有权及累计记录。`validateAuctionLinks` 校验 activeId 与唯一合法 activity/pending、当前地点、开始时间/进度/轮次相符；正在举牌时还核对库存与药鼎资格。待选报价从图录重新构建，忽略原文及注入的 effects/AI 状态。冻结款损坏拒绝整档，而不是重置或尝试猜测退款；这是单机完整性校验，不是防作弊。
'''[1:]),
(139,140,r'''
  schemaVersion: 11,
'''[1:]),
(147,147,r'''
  auction: { day, realm, lots, activeId, wins, spent },
'''[1:]),
(154,155,r'''
存储键：`night-courier:saves:v11`；仅在新键不存在时从 v10、v9、v8、v7、v6、v5、v4、v3 旧键显式迁移（新键的空列表不是缺失，不复活已删除存档）；上一写入备份键：`night-courier:saves:backup`。列表最多12份；每份日志最多180条，防止无界增长。
'''[1:]),
(157,157,r'''

应用 1.6.0 将存档提升到 v11，新增上述拍卖状态。v10及以前初始化空拍卖记录，保留资产与原活动；v11缺失拍卖数据视为损坏。先恢复拍卖图录，再恢复其 pending/activity 并交叉验证，不重复冻结、退还或发物。全部旧格式迁移继续保留，图录按档独立，无离线补算。
'''[1:]),
])

patch('docs/TEST_REPORT.md','bd29efb7843c9a3d791026107b133cfa6e879ff689cf131e7298ddcd1afcfbc6','e4b59915321a9df64838503c8cdbc8d9cd532687e96f686a0e08096867c1c4c7',[
(0,0,r'''
# 1.6.0 万宝拍卖场测试记录

日期：2026-09-19。应用1.6.0，存档v11。基线为 main 52f8c65a3d1dc811352f0d27f1d389e933558240。

本机 Node 22.16.0：`npm run check` 通过，`npm test` 274/274通过（新增31项），`npm run build` 成功。Chromium离线单文件 `python tests/browser-smoke.py` 55/55组通过（新增8组）。人工查看320px的竞价选择与成交面板：没有横向溢出，拍品卡、资金、报价与操作可读。

规则测试覆盖：九/十单与炼药前置、未开启/地点错误/现金不足/不合法报价的原子失败、真实道路到场、六个境界多种种子的三件图录、首鼎限制与药鼎收齐回退、丹方专属渠道不混入、同日图录与对手预算不重抽、18:00及23:55边界、5分钟连续竞价与提交不可中断、超价退款与免费退出、一/三口报价、成交一次性扣费发放、重复事件与拍品请求拒绝、下一日换图录、药鼎收藏不自动切换、大步/细步推进一致。房租测试覆盖同刻现金充足或不足，冻结款不预支；边界还覆盖现金上限中领取现金奖励后退款守恒、库存预留与显示分钟浮点容差。

存档测试覆盖v10迁移保持原资产/活动，新存储键迁移、损坏原文保留、多档隔离；v11图录/冻结款/对手预算/物品/轮次损坏拒绝；在途活动地点/时间/进度交叉校验；待选拍卖从白名单重建，不接受注入的奖励或AI文本；拍卖AI skip不受异步AI响应覆盖。

浏览器新增1200px与320px各四组：受控9/10单夹具检验隐藏导航和伪造按钮防绕过，真实点击沿道路到场及白天图录；真实出价冻结、时间推进、途中返回菜单重载、超价全退与免费放弃；加三口成交、重复按钮不发奖、成交后完整新页面读取；切换新档无拍卖进展污染、旧档加载默认暂停。待选/成交重载使用捕获的localStorage载荷在新浏览器上下文重新加载真实构建，不用跳过规则的内存换值冒充刷新。原47组持续时间、配送、地图、倍率选择DOM稳定、炼药/丹方、渐进解锁及AI回退全部重跑。

环境限制：使用真实Chromium和构建出的单文件，但localStorage、受控时钟、人物进展与对手预算部分为显式夹具，AI输出为模拟；保留独立真实rAF与后台心跳探针，不代表物理手机锁屏、Safari/Firefox或真实AI供应商验证。远端CI和Pages部署以工作流记录为准，不把本机通过当作线上部署成功。

以下为历史版本记录。

'''[1:]),
])

for p,text in writes:
    p.parent.mkdir(parents=True,exist_ok=True)
    p.write_text(text)
