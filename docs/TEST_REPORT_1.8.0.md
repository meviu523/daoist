# 1.8.0 验证记录：凡人单境界与化神后成长

日期：2026-09-19。基线：`cee04abe5bde0018c73dea79ff9024e367237116`（1.7.1，存档 v12）。此次应用版本 1.8.0、存档 v13。

## 本机验证结果

| 命令 | 结果 |
| --- | --- |
| `npm run check` | 通过，服务端及全部前端脚本语法检查 |
| `npm test` | 331/331 通过，无失败或跳过；原 311 项加 20 项境界边界测试 |
| `npm run build` | 通过，生成单文件 `dist/index.html` |
| `python tests/browser-smoke.py` | 69/69 组 Chromium 检查通过，原 61 组加 8 组境界检查 |
| `git diff --check` | 通过 |

## 本次新增覆盖

`tests/realms.test.mjs`：十境82阶段与唯一最高境界；凡人80修为、20体力、60分钟后直接尝试炼气一重；修为/体力不足与渡劫九重拒绝突破，不部分扣费或消耗随机源；失败约75%退款、中断不退款。逐一通过真实 `perform/advance` 验证全部阶段的晋升、下一境界、需求与存读，验证化神九重继续突破及各新增境界概率范围。炼气及以上原属性上限和境界索引保持。

迁移测试遍历 v7–v12 的所有凡人重数以及炼气至化神重数；检查抵扣0/5/11/18/26/35/45/56/68、修为余额和资产保留、日志幂等。旧凡人途中小重分别覆盖成功仅累计旧投入、失败和中断无新增抵扣、已扣丹药和剩余时长保留；原凡人九重及 v5 大境突破可接续。新增高阶破境丹的途中存读、非法境界/抵扣/衔接拒绝、房租优先、旧进阶玩法所有权、v12存储键迁到v13保留原文、新旧多档隔离与空列表不复活旧档。

浏览器新增在1440×1000与390×844视口检查：新档只显示「凡人」；化神→炼虚→合体→大乘→渡劫的九重边界及渡劫九重封顶；旧凡人九重显示凡人且需求12修为；实际点击化神九重突破并重载，保留249修为投入、活动与默认暂停。界面不横向溢出，无 JavaScript 运行异常。已查看手机旧凡人抵扣面板和桌面满级面板截图，布局与标签正常。

原有地图、连续时间、暂停/后台计时、配送及地点发现、房租、炼药/丹方、渐进导航、拍卖和AI白名单回退测试同时回归；本次不修改这些功能的地图、售价、门槛或奖励配置。

## 验证边界

全部阶段晋升测试使用补足修为/体力并控制随机种子的夹具，证明逻辑可达，不代表已用真实游戏经济从零通关或完成长期数值平衡测试。浏览器是本地构建与 Chromium 桌面/手机视口，不等于真实手机设备或线上站点实机测试。AI检查使用模拟响应，未调用付费提供方。未提交测试临时产物、浏览器缓存、用户存档或密钥。

远端 PR CI 与 Pages 部署须在提交后另行检查；本文件记录的是上述本机结果，不把本机构建当作已发布。
