# 1.9.0 事件结果与配送扩充测试记录

日期：2026-09-19。应用1.9.0，存档v14。整合基线main：13a1200059fcf476cee722d19f78822f4d07284e。

`npm run check`、`npm test`、`npm run build`均通过。规则测试525/525项，较1.8.0新增194项；`python tests/browser-smoke.py`通过80/80组，较1.8.0的69组新增11组。未更改CI或Pages的验证要求。

新增规则测试逐个执行60个配送事件的全部180个选择，验证文字结果、零/有耗时、实际资源变化、配送票据一次性结算、首次地点发现、原状态不变、读档往返、阅读冻结、过期确认与重复选择拒绝。其余覆盖数值封顶、选项成本与午夜房租分离、交租失败优先、人物物品成本/好感/信任/章节、主线排队不遮盖结果、探索与修炼结果、AI迟到、v12待选/途中迁移、v14显示白名单/损坏保留/多档隔离，以及受伤后先读结果再继续救助。

浏览器新增1440×1000、390×844、320×740视口，每种三组：实际地图配送后点击选择，显示结果与独立配送到账；确认按钮自动聚焦、Esc不跳过、阅读冻结、没有横向溢出；新文档载入恢复未读结果及已结算资产，确认与重复请求不重奖，已读状态持久化、原手动暂停不被解除；耗时选项途中恢复不重扣，第三单结果先于待选主线，主线选择也有结果。另有320px长AI文本转义和受伤救助结果按钮可用两组。人工查看320px和桌面结果截图，文字、到账及确认入口可读。

本机环境禁止浏览器导航（包括file://），因此新增浏览器恢复测试本机使用全新页面、实际构建脚本与显式持久化存储夹具；不是原生浏览器刷新。测试脚本会优先使用真实file页面、浏览器localStorage与page.reload，只有明确的ERR_BLOCKED_BY_ADMINISTRATOR才回退，并在输出及报告receipt_reload_modes标明模式。GitHub CI环境应走原生路径，其实际模式以该次运行日志为准。

所有浏览器组使用真实Chromium；AI响应为明确模拟，绝大多数时间推进使用受控GameClock，另有真实requestAnimationFrame和后台心跳回归。不是物理手机、Safari/Firefox、真实AI供应商或已部署页面的全面实测。远端PR CI和Pages是否成功，分别以对应工作流结果为准，不能用本机测试代替部署验证。

## 与并行境界更新的整合

保留已合并1.8.0全部境界配置、82阶段规则、旧凡人抵扣与进行中的突破。结果结构独立升至v14；v13境界档无结果字段时补null，不重复执行v12境界迁移。重新执行语法、525项规则、80组浏览器与构建全部通过；新增2项跨版本整合回归覆盖v13四个新境界、凡人抵扣、旧途中突破和存储键优先级。
