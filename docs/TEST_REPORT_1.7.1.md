# 1.7.1 出租屋和修车点初始开放

日期：2026-09-19。基于 main 2cc2ebdb2537d98b1f0a39d65831ba202d29f97b；存档仍为 v12，不覆盖同期 1.7.0 地点解锁实现。

本机 `npm run check`、`npm test`、`npm run build` 通过。规则测试 311/311，Chromium `python tests/browser-smoke.py` 61/61 组通过。远端 Starting garage validation 工作流另行完成语法检查、规则测试和构建。PR CI 与 Pages 部署以其工作流状态为准。

新增覆盖经典/AI 新档只拥有 home/garage、各档地点列表独立、修车铺开放不自动结识陈默；零单沿真实道路抵达修车铺后可维修、升级仍需五单；既有 v12 仅拥有 home 的存档补齐 garage，不改资产、时间、订单、其他地点或日志内容；旧档在修车铺待交付时补权限不提前结算、不额外奖励。

既有地点测试改用仍未开放的书店验证 NPC 与拜访不可绕过；所有七处非初始服务地点仍需配送结算，普通配送地址不常驻。1200px 与 320px 浏览器从真实新档验证小屋和修车点两处常驻、未知订单服务隐藏、配送中断续送与待选读档、结算解锁、取消不解锁及新旧档隔离。其余时间、背景计时、拍卖、炼药、丹方和 AI 回退回归均通过。

浏览器为真实 Chromium，localStorage 与 AI 接口采用明确模拟，主要流程使用受控时钟，另有真实 rAF 和后台心跳检查。不等同于 Safari/Firefox、物理手机锁屏或真实 AI 服务验证。
