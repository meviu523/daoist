# 1.9.0 事件结果与配送扩充

详见 [本次完整测试记录](TEST_REPORT_1.9.0.md)：规则525项、浏览器80组；包含本机存储夹具与CI原生刷新验证的环境区分。

以下为历史版本记录。

# 1.7.0 外卖发现地点测试记录

## 1.8.0 境界调整

本机通过331项规则测试和69组Chromium检查，语法检查、构建与差异空白检查通过。新增20项规则测试及8组界面检查。详细范围、旧档衔接与验证边界见 [1.8.0验证记录](TEST_REPORT_1.8.0.md)。

日期：2026-09-19。应用1.7.0，存档v12。基线为 main e643f5c40d86f24146f93cf958177ac3a51a0c18。

本机 Node 22.16.0：`npm run check` 通过，`npm test` 307/307通过（新增33项），`npm run build` 成功。真实构建的离线单文件 `python tests/browser-smoke.py` 61/61组通过（新增6组）。人工查看320px未知订单和发现后地图截图，服务标记、姓名、时间、日志及导航可读，无横向溢出。

新增规则测试从真实 `newGame` 出发，不使用全地点开放夹具。覆盖初始住处、未知订单不可自由赶路、接单/停止/取消/消失不解锁、实际到达待选不提前授予、真实五分钟配送选项的途中读档和结算、无事件交付分支、八个常驻地点逐一发现、普通住宅记录但不常驻、全部当地操作和快捷入口原子拒绝、地点不跳过玩法、随机人物不授予其所在地、基本生存与救助、午夜房租优先、不重复奖励及AI白名单。100种种子验证保留两个近单及发现单保底，持续配送可发现全部固定设施。

迁移覆盖v1–v11原固定地点与旧普通非配送在途权限保留、当前位置和进度不变；旧普通地址待交付及耗时选择不被误判成功。v12地点白名单、住处、在途赶路/搬家/拜访和当地行动交叉检查；旧存储键保留原文，保存/重载/新档互不污染。旧引擎、地图距离、炼药、拍卖等隔离测试显式开放地点，不用修改新档默认值掩盖门槛。

新增浏览器1200px与320px各三组：原始新档只见小屋常驻点，未知订单仅可接单且隐藏当地服务/搬家；真实沿路移动、中断续送、抵达事件后在新浏览器上下文恢复存储，选择后才产生常驻标记且重复读档不重发；切到第二个新档后取消真实订单不发现地点。既有55组渐进玩法、拍卖、地图、配送、炼药/丹方、AI回退、倍率DOM稳定和前后台时钟全部重跑。

环境说明：运行真实Chromium和构建页面，localStorage与AI接口为显式模拟，主要流程使用受控时钟，另保留真实rAF及后台心跳探针。不是Safari/Firefox、物理手机锁屏或真实AI供应商验证。远端PR CI、Pages状态以相应工作流记录为准，不将本机测试当作线上部署完成。

以下为历史版本记录。

# 1.6.0 万宝拍卖场测试记录

日期：2026-09-19。应用1.6.0，存档v11。基线为 main 52f8c65a3d1dc811352f0d27f1d389e933558240。

本机 Node 22.16.0：`npm run check` 通过，`npm test` 274/274通过（新增31项），`npm run build` 成功。Chromium离线单文件 `python tests/browser-smoke.py` 55/55组通过（新增8组）。人工查看320px的竞价选择与成交面板：没有横向溢出，拍品卡、资金、报价与操作可读。

规则测试覆盖：九/十单与炼药前置、未开启/地点错误/现金不足/不合法报价的原子失败、真实道路到场、六个境界多种种子的三件图录、首鼎限制与药鼎收齐回退、丹方专属渠道不混入、同日图录与对手预算不重抽、18:00及23:55边界、5分钟连续竞价与提交不可中断、超价退款与免费退出、一/三口报价、成交一次性扣费发放、重复事件与拍品请求拒绝、下一日换图录、药鼎收藏不自动切换、大步/细步推进一致。房租测试覆盖同刻现金充足或不足，冻结款不预支；边界还覆盖现金上限中领取现金奖励后退款守恒、库存预留与显示分钟浮点容差。

存档测试覆盖v10迁移保持原资产/活动，新存储键迁移、损坏原文保留、多档隔离；v11图录/冻结款/对手预算/物品/轮次损坏拒绝；在途活动地点/时间/进度交叉校验；待选拍卖从白名单重建，不接受注入的奖励或AI文本；拍卖AI skip不受异步AI响应覆盖。

浏览器新增1200px与320px各四组：受控9/10单夹具检验隐藏导航和伪造按钮防绕过，真实点击沿道路到场及白天图录；真实出价冻结、时间推进、途中返回菜单重载、超价全退与免费放弃；加三口成交、重复按钮不发奖、成交后完整新页面读取；切换新档无拍卖进展污染、旧档加载默认暂停。待选/成交重载使用捕获的localStorage载荷在新浏览器上下文重新加载真实构建，不用跳过规则的内存换值冒充刷新。原47组持续时间、配送、地图、倍率选择DOM稳定、炼药/丹方、渐进解锁及AI回退全部重跑。

环境限制：使用真实Chromium和构建出的单文件，但localStorage、受控时钟、人物进展与对手预算部分为显式夹具，AI输出为模拟；保留独立真实rAF与后台心跳探针，不代表物理手机锁屏、Safari/Firefox或真实AI供应商验证。远端CI和Pages部署以工作流记录为准，不把本机通过当作线上部署成功。

以下为历史版本记录。

# 1.5.0 渐进玩法与导航测试记录

日期：2026-09-19。应用 1.5.0，存档 v10。基线为 main 3530d47b36b060d821ad5955164e9dc37b4cc09d。

本机 Node 22.16.0：`npm run check` 通过、`npm test` 243/243 通过（新增 27 项）、`npm run build` 成功。`python tests/browser-smoke.py` 47/47 组通过（新增 12 组真实新档渐进流程）。人工查看 320px 开局与全导航截图，无页面横向溢出，后期导航可内部滚动。

新增规则覆盖真实 `newGame` 的初始入口、16 类受限指令的原子失败、首单到达/待选/结算、首个委托领奖、首次修炼的中断与途中存档、三单主线与耗时选择结算、同刻房租破产、5 单升级/20 单归途、人物发现、主线篇章可见与防跳领、经典/AI 共用进度、生存路径可达、所有权持久化和只记一次解锁日志。迁移测试覆盖 v9 初始档、已有进展、在途已付费炼药、v10 白名单/前置闭包、旧键迁移/空列表不复活和多存档隔离；108 张丹方的三渠道与未得内容隐藏仍回归。

为区分规则，既有引擎/连续时间/丹方隔离测试的 `fresh` 夹具显式开启功能，新增 `gameplay-unlocks.test.mjs` 不使用该夹具，从真实新档执行实际配送、选择、领奖、修炼和读档。浏览器既有隔离场景同样显式给定已开放状态；新增 1200px 和 320px 各 6 组从原始新档出发：伪造隐藏导航/快捷按钮不能打开面板或改变暂停；第一单实际结算才同时出现系统导航和顶部入口；真实领奖开放吐纳，首次修炼途中重载不提前开启；实际连续三单主线完成出现独立炼药导航；受控 4/5、19/20 单边界、人物发现及全导航触控宽度；切换到第二个新档不残留入口，返回已进展存档保留解锁与默认暂停。原有 35 组地图、持续时间、倍率 DOM 稳定、AI 回退、旧丹方及真实前后台时钟回归均重跑。

浏览器使用构建出的离线单文件与 Chromium；localStorage、受控时钟、部分页面可见性和 AI 输出明确为模拟，并另含真实 rAF 与后台定时器探针。不代表物理手机锁屏、Safari/Firefox 或真实 AI 供应商验证。一次完整浏览器命令被外层 120 秒工具超时终止，未计为通过；后续完整运行 47/47 成功退出。远端 CI 与 Pages 部署以工作流记录为准，不把本机测试计作线上验证。

以下为历史版本记录，早期“开局所有导航/基本系统立即可用”不再代表 1.5.0。

# 1.4.2 丹方差异化获取测试记录

日期：2026-09-19。应用 1.4.2；存档格式保持 v9。基线为 main 11122c911e648aaf4b7d6dda35fbe9dc55b0221d。

本机完成：`npm run check` 通过；`npm test` 216/216 通过（新增 26 项规则测试）；`npm run build` 成功；`python tests/browser-smoke.py` 35/35 组通过（新增 5 组，另增强商店来源检查）。

规则新增覆盖：108 张唯一分配到三类各 36 张；六个主线来源各六张、六位 NPC 各六张、十二种丹药各渠道三张；商店不混入专属方、36 张无重复买齐与售罄原子失败；首个委托必须领奖；五段主线待选/读档不提前发、实际结算与耗时选择、同刻房租破产不发；六位 NPC 的章数和信任边界、真实交谈选项结算、友情路线、信任下降不收回；获得与可炼制独立；所有隐藏 ID 的错误信息不泄露名称；旧档已得专属方保留、补领幂等、多存档隔离及全部 72 张专属方可达。旧“探索直接掉方”测试已更换为遍历种子的防绕过检查。

Chromium 新增实际界面检查：1200px/320px 下买齐商店方仍隐藏专属方并禁用售罄按钮；两种视口显示已遇见 NPC 的传授数量、下一章数与信任，未遇见人物和未得丹方不泄露；剧情待选读档无提前奖励，点击实际选项发六张，重载不重复，并显示正确来源。原有 30 组移动、时钟、倍率选择、后台心跳、充电、地图、暂停、多存档和 AI 回归全部重跑。人工查看 320px 羁绊截图，无横向溢出。

浏览器使用真实离线单文件构建与 Chromium，localStorage、受控时钟、部分可见性属性和 AI 为显式模拟，同时包含真实 rAF/后台心跳探针。不代表真实 AI 供应商、Safari/Firefox 或物理手机锁屏验证。GitHub CI 与 Pages 部署状态以工作流记录为准，不把本机测试记作线上验证。

以下为历史版本记录；其中“探索直接掉方/商店抽取全池”不再代表 1.4.2。

# 1.4.1 丹方获得与隐藏测试记录

日期：2026-09-19。应用 1.4.1，存档格式提升至 v9。

本轮新增独立丹方所有权 `alchemy.formulas`。新存档必须从 0 张丹方开始；炼药指令除原有药鼎、熟练度、境界、药材和灵力校验外，先校验丹方是否已经获得。即使调用方知道隐藏丹方 ID，也不能绕过所有权直接开炉。

长乐集购买改为“未知丹方残卷”：购买前不暴露具体丹方名称、产物、配伍或要求，只显示残卷价格和当前境界仍可获得的未收录数量。购买成功后使用确定性世界随机源从当前境界允许的未收录丹方中选取一张，并永久加入 `alchemy.formulas`。月渡公园和听雨观探索也可能获得尚未收录的丹方。

浏览器回归要求新档进入炼药页时不存在任何 `data-act="alchemy"` 丹方按钮，已知数据表中的未获得丹方名称也不得出现在面板文本中；购入一卷残卷后，只出现刚获得的具体丹方，另一张未获得丹方仍必须隐藏。规则测试同时覆盖购买地点、零耗时、扣费、随机候选范围和隐藏 ID 越权失败。

v8 → v9 迁移测试要求：旧版按当时境界与炼药熟练度已经可使用的丹方自动加入已获得集合；进行中的炼药丹方无条件补入，避免途中存档因新所有权规则损坏。旧药鼎、九阶丹药与其他 v8 结构继续保留。

以下保留 1.4.0 及更早测试记录作为历史基线。

# 1.4.0 炼药体系扩展测试记录

日期：2026-09-19。应用 1.4.0，存档格式提升至 v8。

本轮覆盖炼药内容与存档兼容：固定 64 种药材、108 张独立丹方、24 种药鼎；24 鼎覆盖一至九阶，12 类丹药各生成一至九阶成品，共 108 个阶位成品。测试明确区分“丹方”和“丹药”：丹方保存产物、药材配伍、灵力、火候、复杂度、熟练度/境界门槛与最低药鼎阶位，不直接固定成品阶数。

规则检查覆盖：炼药开炉一次性校验并扣除丹方所需的全部药材与灵力，条件失败不得部分扣料；中断不返还已投入药材且不增加熟练度；丹药阶数上限同时受当前药鼎阶位与炼药师阶位限制，任一方不足都不能越阶，悟性不直接提高丹药阶数，但继续提高成丹率与额外产出概率；24 种药鼎可以独立购买、拥有和切换，同阶不同器型的成丹、额外产出与成色修正分别生效；高阶破境丹按阶位提供不同突破加成。

存档检查覆盖 v7 → v8 药鼎迁移：旧青铜、赤铜、玄铁三档索引显式映射到新 24 鼎中的对应器型并加入已拥有集合；旧 `heal/stamina/qi/foundation` 库存继续作为一阶丹药；新增药材、高阶丹与药鼎拥有字段按白名单补齐。旧 v5 正在炼药的存档仍可补基础药鼎并恢复当前丹方，不重复扣除已经投入的材料。

Chromium 回归实际前往长乐集购买药鼎，确认基础丹方可开炉、按多药材配伍扣料，并在浏览器运行时断言 64 种药材、108 张丹方、24 种药鼎的目录数量。PR #13 当前功能提交已通过 `npm run check`、`npm test`、`npm run build` 与 `python tests/browser-smoke.py`；最终文档提交继续由同一 CI 全量重跑。

以下保留 1.3.4 及更早测试记录作为历史基线。

# 1.3.4 时间倍率选择框稳定性测试记录

日期：2026-09-19。应用 1.3.4，存档格式仍为 v7。

新增 Chromium 回归覆盖世界 `revision` 对顶部时间控件的影响：创建存档后设为 10 倍、聚焦 `#time-speed`，保存该 select 与 `.time-controls` 的 DOM 引用，再推进跨过下一次 15 分钟订单刷新边界。刷新后要求两个节点引用均未变化、`document.activeElement` 仍为原选择框、倍率值仍为 10。该检查直接防止完整状态刷新通过重写 header 关闭原生选择框。

PR #12 首轮完整 CI 已通过 `npm run check`、`npm test`（183/183）、`npm run build` 与 Chromium 30/30 组检查。

以下保留 1.3.3 及更早测试记录作为历史基线。

# 1.3.3 配送点进度环对齐修复测试记录

日期：2026-09-19。应用 1.3.3，存档格式仍为 v7。

修复 SVG 圆环使用 CSS 旋转中心导致的跨浏览器/缩放错位。现在圆环显式使用 `cx=0`、`cy=0` 和 `rotate(-90 0 0)`。Chromium 回归除继续检查环形进度、出现和消失动画外，还比较配送点核心圆与进度环的屏幕中心坐标，横纵误差均必须小于 0.5px。

以下保留 1.3.2 及更早测试记录作为历史基线。

# 1.3.2 订单消失时间与地图进度环测试记录

日期：2026-09-19。应用 1.3.2，存档格式仍为 v7。

## 1.3.2：订单消失时间与配送点动画（最新）

本轮将配送“送达时限”改为未接订单的地图消失时间。规则检查覆盖：消失时刻不能再接单、只要消失前成功接单就不要求在原时间前抵达、已接订单在原消失时间之后仍能停止/继续、灵异订单不会重复扣灵力、最终现金与外卖币不再因耗时打折。旧 `expiresAt` 字段仅保留作为待接单消失时刻，schemaVersion 不变。

地图 Chromium 检查新增圆形进度环：游戏时间推进后 `stroke-dashoffset` 连续增加，表示剩余比例缩短；新订单内层视觉组使用 `orderPointIn` 淡入弹出，订单消失时先撤销交互再使用 `orderPointOut` 缩小淡出，动画结束后节点移除。减少动态效果偏好下直接移除，不改变规则。

PR #10 的首轮完整 CI 在 Ubuntu 24.04 / Node.js 22.16.0 / Python 3.12 / Playwright Chromium 环境验证：
- `npm run check`：通过。
- `npm test`：183/183 通过，0 失败。
- `npm run build`：成功生成单文件构建。
- `python tests/browser-smoke.py`：29/29 组通过，0 失败。
- 同时重跑 110 地点、后台持续计时、10 分钟充电、九重境界、炼药、存档恢复、移动路线、AI 模拟与移动端视口回归。

测试中的 localStorage 与 AI fetch 仍为显式模拟；Chromium 为真实构建页面和真实 DOM/CSS 动画，但不等于 Safari/Firefox、物理手机系统冻结或真实付费 AI 联调。线上状态以合并后的 GitHub Pages 工作流为准。

以下保留 1.3.1 及更早测试记录作为历史基线。

# 1.3.1 扩图与后台计时测试记录

## 1.3.1：110 地点与后台持续计时

在 1.3.0 基础上继续向南扩展南岸新街，追加 11 个动态配送地址，总计 110 地点、11×10 路网、2660×1860 世界。保留已发布 99 地点的 ID、名称、坐标和全部 9801 对最短路线，四座桥不变。相较最初 42 地点版本，面积约为 2.55 倍。普通配送点仍只随订单出现，手机可查看完整地图，开局默认定位玩家。

取消页面隐藏与长帧自动暂停：前台动画回调和隐藏页每秒心跳共用单调时钟，重叠回调不重复计时。浏览器延迟后台回调时，分批推进仍存活会话经过的时间；每次最多四批、每批 60 游戏分钟，余量留待下一次。手动暂停、面板、事件、菜单、冲突和终局仍独立生效；遇到剧情立即停止并清空余量，不自动代选，订单和房租照常结算。关闭/重载后不补算离线时间，读档仍默认暂停，不保证浏览器或操作系统后台保活。

保留 10 分钟 / ¥8 充电、六大境界各九重、买鼎炼药与熟练度、到达后清除已完成路线，以及 v7 存档迁移。本次未改动规则引擎、存档迁移或成长模块，不需要重开存档。

本地 `npm run check`、`npm test`（182/182）、`npm run build` 和 Chromium 28 组检查通过。浏览器包括真实动画循环与禁用动画后的真实后台定时器探针；隐藏属性、存储和 AI 明确模拟，不代表物理手机锁屏、页面被系统销毁恢复或付费模型联调。

本轮基线为 main 401b782（PR #8），保留既有 167 项规则检查与 25 组浏览器覆盖，更新冲突的旧后台暂停断言；新增 15 项规则检查及 3 组浏览器检查，总计 182 / 28。新夹具为固定种子生成的合成旧城存档，不是用户数据。

以下为历史 1.3.0 测试报告，后台暂停描述不再代表当前实现。线上 CI/Pages 以本次工作流实际结果为准。

# 1.3.0 九重境界、炼药师与页面连续运行测试记录

日期：2026-09-18。应用 1.3.0，存档格式 v7。基线保留 1.2.0 的 11×9 / 99 地点、四座桥与 ¥8 / 10 分钟原地充电。

## CI 执行结果

PR #8 的 GitHub Actions 在 Linux / Node.js 22.16.0 / Python 3.12 / Playwright Chromium 环境执行：

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| JavaScript 语法 | `npm run check` | 通过 |
| 规则、存档和真实本机 HTTP | `npm test` | 167 通过，0 失败，0 跳过 |
| 静态单文件构建 | `npm run build` | 成功生成 `dist/index.html` |
| Chromium 界面 | `python tests/browser-smoke.py` | 25 组通过，0 失败 |

该轮 CI 同时重新覆盖 1.2.0 的扩城、道路缓存和充电迁移测试，不是只运行新增用例。此前 `docs/test-results/expansion-*` 是 1.2.0 的历史快照；1.3.0 的权威结果以 PR #8 的 CI 日志为准。

## 1.3.0 新增回归

### 九重小境界

六个大境界均验证一重至九重。规则测试覆盖当前重数的修为需求、成功/失败路径、护身符与破境丹、提前停止不退投入，以及九重成功后进入下一大境界一重。化神九重封顶；主线、系统解锁和结局继续使用大境界门槛，避免既有内容因小境界拆分而失去可达性。

40 单自动游玩继续执行，突破判断改用 `G.realmNeed(s)`，验证新增九重结构下主线与守城结局仍可达。

### 炼药师与药鼎

规则测试覆盖未购鼎禁止炼药、只能在长乐集购买/升级药鼎、境界限制、高阶鼎提高成丹率与额外产出概率、灵草/灵力预付、中断不返还、完成一炉才增加熟练度，以及熟练度逐步解锁回春丹、清心散、凝气丹和破境丹。

Chromium 用例不直接篡改角色位置：实际从地图前往长乐集，打开修行面板购买青铜药鼎，再开炉炼制回春丹，检查现金、灵草、炼药次数和熟练度变化。

### 页面隐藏不再自动暂停

`visibilitychange/pagehide/pageshow` 只保存并重置墙钟基准，不新增 `manual` 暂停理由。浏览器隐藏期间不推进模拟；恢复后保持离开前的运行/暂停状态，不补算后台墙钟时间。独立单测同时确认 `GameClock.reset()` 不改变暂停理由。

通用“非页面隐藏的异常长帧”保护仍保留：渲染间隔异常超过 1 秒时进入手动暂停，避免意外批量追赶。

### 到达后清除旧路线

地图只绘制当前选中路线或正在移动的剩余路线，不再用 `lastRoute` 绘制已完成轨迹。Chromium 配送流程在抵达并弹出事件后断言 `.trail-layer path` 已清空；存档中的历史路线字段仍可保留，不影响道路兼容。

## 保留的 1.2.0 扩城与快速充电回归

`tests/expansion.test.mjs` 继续覆盖 ¥8 / 10 分钟原地充电、按缺口逐步恢复、满电/欠费/忙碌拒绝、道路途中充电、中断不退款、暂停及多次读档不重复扣费或补电。旧 v5 的 30 分钟充电按完成比例换算至 10 分钟，保留游戏时间、现有电量、恢复快照与已支付费用。

地图保持 99 节点 / 173 道路边 / 四座桥；57 个扩展地址 ID、名称和坐标唯一，普通地址不常驻。旧 42 地点、旧三座桥、1764 条地点间路线与 2856 条旧路段中点路线继续使用基线摘要校验，防止本次修仙玩法改动回退地图或破坏途中存档。

## v7 存档兼容

新键为 `night-courier:saves:v7`，识别重建版 v1–v7。旧存档没有 `realmLevel` 时从原大境界一重继续；若 v5/v6 保存的是旧规则下已经开始的大境突破，则按当前大境界九重恢复，并保留旧版整阶修为投入，使目标仍是下一大境界。

旧炼药途中存档没有炼药字段时补一口基础药鼎，仅用于让当前炉合法完成；其他旧档默认未购鼎。v5 的 30→10 分钟充电迁移与 99 地图旧路线兼容继续同时生效。损坏位置、路线、行动类型和未来版本仍被拒绝，不静默修成可加载状态。

## Chromium 25 组检查

核心流程包括：

- 开局显示“凡人一重”，未购鼎时炼药按钮禁用。
- 空闲连续计时、全局暂停、接单真实移动、途中小数坐标与存档恢复。
- 到达目的地后旧路线立即消失；配送事件和奖励只结算一次。
- 面板暂停、手动暂停和 1/3/10 倍速度组合互不串扰。
- 页面隐藏不新增手动暂停，恢复后继续原状态且不补算离开时间。
- 实际前往长乐集买鼎炼药，验证资源与熟练度。
- 320/390 像素全城视图、双指缩放、导航/面板/行动栏无横向溢出。
- 99 地点共享道路 SVG 与拓扑一致；10 分钟充电的 UI、连续进度、暂停和读档一致。
- 新区订单通过实际 UI 接取、移动、途中读档、交付并移除任务点。
- AI 模拟成功、失败回退、迟到响应取消；等待和阅读期间冻结。
- 真实 requestAnimationFrame 探针验证空闲流逝、移动与手动暂停。

测试采用真实构建页面、DOM 和 Chromium 交互，但 localStorage 与 AI fetch 在浏览器脚本中显式模拟；页面隐藏通过可见性事件模拟，不等于真实手机操作系统冻结。

## 范围限制

未验证真实付费模型、Safari/Firefox、物理 iOS/Android、系统级省电冻结、真实浏览器磁盘配额耗尽、高并发多标签写入或未知原仓库存档。线上部署状态以合并后对应 GitHub Pages 工作流为准，不由 CI 测试推断。

## 复验

```sh
npm run check
npm test
npm run build
python tests/browser-smoke.py
```

浏览器测试需要 Python Playwright 与 Chromium，仅用于开发验证，不是运行游戏的依赖。
