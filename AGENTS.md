# 外卖修仙：仓库协作规则

开始修改前阅读本文件、`docs/ARCHITECTURE.md`、`PROJECT_MEMORY.md` 和相关源码。需求与设计冲突时先说明，再按用户最新确认的方向同步修改设计与实现。

## 必须保持

- 原生 JavaScript IIFE；数据和接口注册到 `globalThis.NightCourier`。不要在界面中写余额、位置、奖励或模拟时间。
- `G.perform` 负责校验并开始行动/即时事务；`G.advance` 是唯一持续推进入口，使用游戏分钟（允许小数）。无效指令不得部分扣费。
- 时间、沿路坐标、消耗和收益同步。不得增加瞬移、直接跳到行动结束、关闭/重载页面后的离线补算、每帧随机抽奖或按帧固定距离。
- 同时一个主要行动；暂停与停止行动不同；手动/面板/事件/菜单/冲突暂停相互独立。页面隐藏不新增暂停理由；统一时钟在前台和后台推进同一会话时间，长间隔分批处理，遇到事件或主动暂停立即停止。关闭/重载无离线补算，读档默认暂停。
- 扩图不得重排已发布地点 ID 或移动旧坐标。道路绘制、寻路、途中校验共用 `ROAD_NODES/ROAD_EDGES`；充电费用和时长共用 `G.CHARGE`。
- 地图接单，不增加取餐环节。已接订单与地图待接单分开保存。系统兑换即时、零耗时、无地点要求。
- 保留电动车与原地充电；维修升级需要修车铺。六个大境界均细分一重至九重，突破先升小境界，九重再跨大境界，化神九重封顶。炼药须先在长乐集购鼎，鼎阶/悟性/熟练度影响成丹率与额外产出。NPC 仅结识后显示。日志只集中在右下角。
- 第 7、14、21… 日零点先扣当前住处房租，再处理同刻抵达/交付/入住。现金不足立即结束，不预支奖励。
- 开局选择经典/AI 模式，局内不能改；关键主线保持经典。AI 输出受白名单约束，不执行文本、不暴露密钥。
- 显式存档迁移、途中状态校验与多存档自动保存，不静默覆盖损坏存档。单机校验不是防作弊。

## 验证与交付

执行 `npm run check && npm test && npm run build`，界面改动还需 `python tests/browser-smoke.py`（开发依赖：Playwright、Chromium）。新规则同时新增边界测试，更新架构、项目记忆与测试记录。只修改 `public` 源码，不只改 `dist`。

按用户要求在功能分支提交，检查 CI 后 squash merge，并验证 Pages 工作流是否成功。区分本机测试、模拟 AI、浏览器视口与真实线上部署；未完成的验证如实说明。不要提交 `.env`、API 密钥、用户存档或临时产物。
