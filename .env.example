# 经典剧情不需要设置以下变量。AI 模式未配置或调用失败时自动使用经典事件。
# 密钥只留在本机服务端，切勿上传 .env 或将其放入 public/。
HOST=127.0.0.1
PORT=8787
# 填写你所用服务的完整 Chat Completions 接口地址（必须 HTTPS）。
AI_API_URL=
AI_API_KEY=
AI_MODEL=
# 只有自己控制的本机模型端点才可使用 HTTP；不会允许任意外网 HTTP。
AI_ALLOW_LOCAL_HTTP=false
